<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <b><%= link_to "SORTFOLIO", root_path, class: "navbar-brand" %></b>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to 'Add studio', new_studio_path %></li>
          <li><%= link_to 'Account', edit_user_registration_path %></li>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Login", new_user_session_path, class: "active" %></li>
        <% end %>
         <ul class="nav navbar-nav">

            <%= simple_form_for "" , :method => :get do |f| %>
            <li><%= f.input :min_price, as: :integer, label: 'Min price', required: false %></li>
            <li><%= f.input :max_price, as: :integer, label: 'Max price', required: false %></li>
            <li><%= f.input :city, required: false%></li>
            <li><%= f.button :submit, 'Search'%></li>
         <%end%>
         </ul>
      </ul>
    </div><!-- /.navbar-collapse -->

  </div><!-- /.container-fluid -->
</nav>
